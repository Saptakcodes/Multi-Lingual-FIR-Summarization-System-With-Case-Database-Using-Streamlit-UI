# Multi-Lingual-FIR-Summarization-System-With-Case-Database-Using-Streamlit-UI
FIR Sahayak: Fast, accurate AI e-Police Desk. Upload FIR → OCR, summarization, 12-language translation. Natural language search by date, FIR#, name, complainant/accused. Local DB with advanced filters. Streamlit UI, Python/FastAPI, Tesseract OCR, Qwen model. Fully offline, zero external APIs. Secure case management for law enforcement.

FIR Sahayak is an AI-powered e-Police Desk for FIR intake, OCR, summarization & multilingual translation. Upload any FIR document → AI extracts text, generates structured summary, translates into 12 Indian languages. The smart assistant enables natural language search across cases by date, FIR number, complainant name, accused name, or free-text queries like "show FIRs between 1 Jan 2025 and 15 June 2025". All records are stored locally with advanced filtering. Streamlit web interface with dark police-style theme ensures intuitive case management. Built with Python, FastAPI, Tesseract OCR, Qwen summarization model & NLP date parser. Secure, offline-ready, zero third-party API dependency.  will it work on offline We need to respond to the user's question: "will it work on offline". The user just asked after we provided the project description. We need to confirm offline capability.  From the description: "Secure, offline-ready, zero third-party API dependency." So yes, it works offline because it uses local models, OCR, and database. However, we should clarify that the AI summarization model must be downloaded once, and then it runs locally. The entire system can run without internet.  We should provide a reassuring answer.  Yes, FIR Sahayak runs completely offline once installed.  Here's why:  All AI models are local – Qwen summarization, Tesseract OCR, and translation models are downloaded once and run on your machine.  No external APIs – No cloud calls for OCR, summarization, translation, or search.  Local database – SQLite stores all FIR records securely on your own system.  No internet required after initial setup – the entire system (backend + frontend) works in air‑gapped environments.  Setup steps for offline use:  Download and place the summarization model (./models/qwen-fir-summarizer-final) – provided separately or trained locally.  Install dependencies (Python, Tesseract, Poppler) – all available as offline installers.  Run the backend (uvicorn) and frontend (streamlit) – both work without external connectivity.  Perfect for police stations, courtrooms, or any secure facility where internet is restricted.
